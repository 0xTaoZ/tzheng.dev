---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="System Design | tzheng.dev" description="How tzheng.dev evolved from a custom cyber portfolio into a versioned, content-driven static engineering system.">
  
  <!-- Cursor glow elements (same as index.html) -->
  <div id="sys-cursor-field" class="sys-cursor-field" aria-hidden="true"></div>
  <div id="sys-cursor-dot" class="sys-cursor-dot" aria-hidden="true"></div>
  <!-- Ambient particle canvas -->
  <canvas id="sys-ambient" class="sys-ambient-canvas" aria-hidden="true"></canvas>

  <header class="sys-hero shell">
    <div class="hero-grid">
      <div class="hero-narrative">
        <span class="eyebrow">BEHIND THIS SITE</span>
        <h1 class="sys-title">System Design.</h1>
        <p class="sys-subtitle">How tzheng.dev evolved from a custom cyber portfolio into a versioned, content-driven static engineering system.</p>
        <div class="narrative-text">
          <p>This is not a list of technologies.</p>
          <p>It is the story of how the site was designed, how its architecture changed, how content moves from source files to production, what broke during the migration, and why some forms of complexity were deliberately left out.</p>
        </div>
      </div>
      <aside class="hero-profile">
        <div class="sys-card profile-card" data-tilt>
          <div class="sys-card-inner">
            <h3 class="profile-heading">SYSTEM PROFILE</h3>
            <div class="profile-grid">
              <div class="profile-fact"><b>RUNTIME MODEL</b><span>Static</span></div>
              <div class="profile-fact"><b>FRAMEWORK</b><span>Astro 5</span></div>
              <div class="profile-fact"><b>CONTENT</b><span>MDX Collections</span></div>
              <div class="profile-fact"><b>BUILD</b><span>Node 24</span></div>
              <div class="profile-fact"><b>SOURCE OF TRUTH</b><span>Git</span></div>
              <div class="profile-fact"><b>DELIVERY</b><span>GitHub Pages &rarr; Cloudflare Edge</span></div>
            </div>
          </div>
        </div>
      </aside>
    </div>
    <div class="hero-tags">
      <span class="chip">Astro 5</span>
      <span class="chip">TypeScript</span>
      <span class="chip">MDX</span>
      <span class="chip">Git</span>
      <span class="chip">CI/CD</span>
      <span class="chip">Static Delivery</span>
    </div>
  </header>

  <!-- Mobile Chapter Bar -->
  <nav class="chapter-bar sys-lg-hidden sys-glass-bar" aria-label="Chapters Mobile">
    <ul class="chapter-list-mobile">
      <li><a href="#origin" class="ch-link">01 Origin</a></li>
      <li><a href="#architecture" class="ch-link">02 Arch</a></li>
      <li><a href="#content" class="ch-link">03 Content</a></li>
      <li><a href="#delivery" class="ch-link">04 Delivery</a></li>
      <li><a href="#interface" class="ch-link">05 Interface</a></li>
      <li><a href="#problems" class="ch-link">06 Problems</a></li>
      <li><a href="#security" class="ch-link">07 Security</a></li>
    </ul>
  </nav>

  <div class="sys-body shell">
    <!-- Desktop Story Rail -->
    <aside class="rail-wrapper">
      <nav class="story-rail" aria-label="Chapters Desktop">
        <div class="rail-label">THE STORY RAIL</div>
        <div class="rail-track">
          <ul class="rail-list">
            <li><a href="#origin" class="ch-link" aria-current="page">Origin</a></li>
            <li><a href="#architecture" class="ch-link">Architecture</a></li>
            <li><a href="#content" class="ch-link">Content</a></li>
            <li><a href="#delivery" class="ch-link">Delivery</a></li>
            <li><a href="#interface" class="ch-link">Interface</a></li>
            <li><a href="#problems" class="ch-link">Problems</a></li>
            <li><a href="#security" class="ch-link">Security</a></li>
          </ul>
        </div>
      </nav>
    </aside>

    <main class="story-main">
      
      <!-- 01 ORIGIN -->
      <section id="origin" class="chapter reveal">
        <h2 class="ch-kicker">01 &mdash; From Interface to System</h2>
        <h3 class="ch-heading">The interface could grow. The content model could not.</h3>
        <p>tzheng.dev began as a highly customized personal homepage.</p>
        <p>The original architecture was built around visual identity: custom HTML, CSS, JavaScript, motion, terminal-inspired interactions, project presentations, and a deliberately cinematic first impression.</p>
        <p>That model worked well while the website was primarily a portfolio. Then the site began to grow.</p>
        <p>Projects needed independent records. Articles needed stable routes, metadata, categories, and long-form layouts.</p>
        <p>Search engines needed canonical URLs, structured identity signals, sitemap output, and pages that could exist independently from the homepage.</p>
        <p>The problem was no longer visual. The interface could continue growing, but the content model could not. Every new long-term feature increased the risk of turning one presentation layer into the source of truth for the entire site.</p>

        <div class="callout-block">Keep the expressive interface. Rebuild the system underneath it.</div>
        
        <p>The homepage remained the identity layer.</p>
        <p>Articles and projects became structured content.</p>
        <p>Astro became responsible for routing, content generation, metadata, and production builds.</p>
        <p>Git became the history of the system.</p>
        <p>The objective was not to replace the original site with a framework. It was to separate the parts that needed to evolve independently. The result is a hybrid system: an expressive shell on top of a structured core.</p>

        <div class="sys-card reveal" data-tilt>
          <div class="sys-card-inner ba-grid">
            <div class="ba-col">
              <div class="ba-label">BEFORE</div>
              <ul>
                <li>Custom homepage</li>
                <li>Hardcoded content</li>
                <li>Direct page edits</li>
                <li>Presentation owns structure</li>
                <li>Source and published output are nearly the same thing</li>
              </ul>
            </div>
            <div class="ba-arrow">&rarr;</div>
            <div class="ba-col ba-after">
              <div class="ba-label ba-label-accent">AFTER</div>
              <ul>
                <li>Identity layer</li>
                <li>Structured content</li>
                <li>Generated routes</li>
                <li>Validated content model</li>
                <li>Explicit build system</li>
                <li>Deployable artifact</li>
              </ul>
            </div>
          </div>
        </div>
      </section>

      <!-- 02 ARCHITECTURE -->
      <section id="architecture" class="chapter reveal">
        <h2 class="ch-kicker">02 &mdash; The Architecture</h2>
        <h3 class="ch-heading">Static by design, not by limitation.</h3>
        <p>Most content on tzheng.dev is public and read-heavy.</p>
        <p>Visitors do not need accounts to read a project. An article does not need a database query on every request. A portfolio does not need an authenticated application runtime merely because such a stack is available.</p>
        <p>That made static generation the natural default. Astro owns the structural layer of the site.</p>
        
        <div class="sys-card reveal" data-tilt>
          <div class="sys-card-inner">
            <p class="arch-intro"><strong>Astro owns the structural layer:</strong></p>
            <div class="chip-row">
              <span class="chip">routing</span>
              <span class="chip">content collections</span>
              <span class="chip">shared layouts</span>
              <span class="chip">metadata</span>
              <span class="chip">RSS generation</span>
              <span class="chip">sitemap generation</span>
              <span class="chip">production builds</span>
            </div>
          </div>
        </div>

        <p>MDX stores long-form articles and project records. TypeScript supports the configuration and content tooling around the build. The original homepage keeps the custom frontend behavior that gives it its identity.</p>
        <p>This boundary is deliberate.</p>

        <div class="callout-block">The framework does not need to own every interaction in order to own the architecture.</div>

        <p>The system can be understood as a sequence of transformations:</p>

        <div class="sys-card reveal" data-tilt>
          <div class="sys-card-inner flow-row">
            <div class="flow-node">
              <h4>AUTHORING</h4>
              <span>Astro pages &middot; MDX &middot; HTML/CSS/JS</span>
            </div>
            <div class="flow-sep">&rarr;</div>
            <div class="flow-node flow-highlight">
              <h4>SOURCE OF TRUTH</h4>
              <span>Git repository &middot; GitHub</span>
            </div>
            <div class="flow-sep">&rarr;</div>
            <div class="flow-node">
              <h4>CI</h4>
              <span>GitHub Actions &middot; Node 24 &middot; npm ci</span>
            </div>
            <div class="flow-sep">&rarr;</div>
            <div class="flow-node">
              <h4>BUILD</h4>
              <span>Astro &middot; npm run build</span>
            </div>
            <div class="flow-sep">&rarr;</div>
            <div class="flow-node">
              <h4>ARTIFACT</h4>
              <span>dist/</span>
            </div>
            <div class="flow-sep">&rarr;</div>
            <div class="flow-node">
              <h4>HOSTING</h4>
              <span>GitHub Pages</span>
            </div>
            <div class="flow-sep">&rarr;</div>
            <div class="flow-node">
              <h4>EDGE</h4>
              <span>Cloudflare</span>
            </div>
            <div class="flow-sep">&rarr;</div>
            <div class="flow-node">
              <h4>VISITOR</h4>
              <span>Browser</span>
            </div>
          </div>
        </div>

        <p>This is not a zero-JavaScript website. JavaScript remains where interaction, navigation, motion, and interface behavior justify it. The distinction is more important:</p>
        
        <div class="callout-block">JavaScript is a deliberate layer, not the default answer to every page.</div>
        
        <p>The public system remains mostly static, while the expressive parts of the frontend are allowed to remain expressive.</p>
      </section>

      <!-- 03 CONTENT -->
      <section id="content" class="chapter reveal">
        <h2 class="ch-kicker">03 &mdash; Content as a System</h2>
        <h3 class="ch-heading">A new article should be a content file, not homepage surgery.</h3>
        <p>One of the most important architectural changes was separating content from presentation.</p>
        <p>Articles and projects are no longer treated as permanent fragments inside a large homepage. They exist as independent Markdown and MDX documents inside structured collections.</p>

        <div class="sys-card reveal" data-tilt style="margin: 2rem 0;">
          <div class="sys-card-inner">
            <h4 class="schema-title">DIRECTORY STRUCTURE</h4>
            <pre class="dir-tree"><code>src/content/
├── config.ts
├── articles/   (8 files)
└── projects/   (4 files)</code></pre>
          </div>
        </div>

        <p>Each content type follows a strict Zod schema defined in <code>config.ts</code>.</p>

        <div class="schema-row">
          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <h4 class="schema-title">ARTICLE SCHEMA</h4>
              <pre><code>title: z.string()
slug: z.string().optional()
date: z.date()
updated: z.date().optional()
excerpt: z.string()
tags: z.array(z.string()).default([])
category: z.string()
featured: z.boolean().default(false)
status: z.enum(["draft", "published", "archived"]).default("published")
readingTime: z.string()</code></pre>
            </div>
          </div>
          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <h4 class="schema-title">PROJECT SCHEMA</h4>
              <pre><code>title: z.string()
slug: z.string().optional()
date: z.date()
status: z.enum(["active", "archived", "learning", "open-source", "security"])
type: z.string()
featured: z.boolean().default(false)
priority: z.number().default(99)
stack: z.array(z.string()).default([])
github: z.string().url().optional()
demo: z.string().url().optional()
excerpt: z.string()</code></pre>
            </div>
          </div>
        </div>

        <a class="evidence-link" href="https://github.com/0xTaoZ/tzheng.dev/blob/main/src/content/config.ts" target="_blank" rel="noopener noreferrer">
          View content schema &nearr;
        </a>

        <p style="margin-top: 2rem;">The content model creates a boundary between what exists and how it is presented. The entire transformation pipeline ensures content is validated before it ever reaches a layout:</p>

        <div class="sys-card reveal" data-tilt>
          <div class="sys-card-inner pipeline">
            <div class="pipe-node">MDX FILE</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">SCHEMA VALIDATION</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">CONTENT COLLECTION</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">GENERATED ROUTE</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">SHARED LAYOUT</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node pipe-accent">STATIC PAGE</div>
          </div>
        </div>

        <p style="margin-top: 1.5rem;">This makes content changes predictable. A malformed content record can be detected before publication instead of silently becoming part of a page. A new article does not require editing the homepage. A project can change its metadata without rewriting its layout.</p>
        <p>CONTENT decides what exists. LAYOUT decides how it is presented. BUILD decides how it becomes a page.</p>
      </section>

      <!-- 04 DELIVERY -->
      <section id="delivery" class="chapter reveal">
        <h2 class="ch-kicker">04 &mdash; From Commit to Production</h2>
        <h3 class="ch-heading">The repository is the source. <code>dist/</code> is the product.</h3>
        <p>Every meaningful change begins in Git. Code, content, configuration, metadata, and interface changes enter the same version-controlled history. That makes changes reviewable, traceable, and reversible.</p>
        <p>But moving from directly publishable files to Astro introduced an important new boundary. The repository itself is no longer the finished website. It is source code. The production website has to be built.</p>
        
        <div class="sys-card reveal" data-tilt style="margin: 2rem 0;">
          <div class="sys-card-inner pipeline">
            <div class="pipe-node">PUSH TO MAIN</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">GITHUB ACTIONS</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">CHECKOUT (actions/checkout@v4)</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">NODE 24 (actions/setup-node@v4)</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">npm ci</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">npm run build</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">dist/</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">UPLOAD PAGES ARTIFACT</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">DEPLOY TO GITHUB PAGES</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">CLOUDFLARE EDGE</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node pipe-accent">VISITOR</div>
          </div>
        </div>

        <a class="evidence-link" href="https://github.com/0xTaoZ/tzheng.dev/blob/main/.github/workflows/deploy.yml" target="_blank" rel="noopener noreferrer">
          View deployment workflow &nearr;
        </a>

        <p style="margin-top: 2rem;">A commit can succeed while the build fails. A build can succeed while the wrong artifact is deployed. A deployment can succeed while a route or client interaction is still broken.</p>
        <p>Treating source, build, artifact, and deployment as separate failure boundaries makes issues easier to locate. The pipeline does not become more professional by having more stages. It becomes more useful when each stage has a clear purpose.</p>
      </section>

      <!-- 05 INTERFACE -->
      <section id="interface" class="chapter reveal">
        <h2 class="ch-kicker">05 &mdash; The Interface Layer</h2>
        <h3 class="ch-heading">Preserve identity without letting the interface own the architecture.</h3>
        <p>The website has two different jobs. The first is to create identity. The second is to carry information. Those jobs do not need the same visual intensity.</p>
        <p>The homepage is intentionally expressive. It uses a dark visual system, terminal-inspired details, controlled motion, custom interactions, and a stronger sense of atmosphere. It is designed to be remembered.</p>
        <p>Articles, project case studies, and system pages have a different responsibility. They need to remain readable for longer periods. They need predictable spacing. They need stable typography. They need layouts that survive different amounts of content.</p>

        <div class="sys-card reveal" data-tilt style="margin: 2.5rem 0;">
          <div class="sys-card-inner layer-stack">
            <div class="layer-box layer-identity">
              <h4>IDENTITY LAYER</h4>
              <p>cinematic &middot; interactive &middot; motion-aware &middot; brand-heavy &middot; terminal-inspired</p>
            </div>
            <div class="layer-sep">&updownarrow;</div>
            <div class="layer-box layer-content">
              <h4>CONTENT LAYER</h4>
              <p>structured &middot; readable &middot; predictable &middot; content-driven &middot; schema-validated</p>
            </div>
          </div>
        </div>

        <p>It is not one interface repeated everywhere. It is one design language with different levels of expression.</p>
        <p>Shared interface patterns create continuity between those layers: navigation, metadata, chips, typography hierarchy, spacing rules, cards, footer signals, and the interaction language.</p>
        <p>The design system is deliberately lightweight. The project does not introduce an external UI framework simply to prove that one could be used. Custom CSS remains appropriate where the visual system is specific to the site. Astro provides structure where structure is needed.</p>
        
        <h4 class="ch-sub">RESPONSIVE TRANSFORMATION</h4>
        
        <p>The page architecture follows another important rule:</p>
        <div class="callout-block">Desktop and mobile are not scaled versions of the same composition.</div>
        
        <div class="sys-card reveal" data-tilt style="margin: 2rem 0;">
          <div class="sys-card-inner transform-grid">
            <div class="transform-col">
              <div class="transform-label">DESKTOP</div>
              <ul>
                <li>Persistent Story Rail</li>
                <li>Horizontal Architecture Flow</li>
                <li>Wide Comparison Layouts</li>
                <li>Hover-aware Enhancements</li>
                <li>Persistent Context</li>
              </ul>
            </div>
            <div class="transform-col transform-accent">
              <div class="transform-label transform-label-accent">MOBILE</div>
              <ul>
                <li>Sticky Chapter Bar</li>
                <li>Vertical System Flow</li>
                <li>Stacked Comparisons</li>
                <li>Touch-first Interaction</li>
                <li>Prioritized Reading Order</li>
              </ul>
            </div>
          </div>
        </div>

        <p>Desktop layouts can use horizontal relationships, persistent navigation, architecture maps, and wider comparisons. Mobile layouts need a different reading order. Complex horizontal diagrams become vertical flows. Persistent side navigation becomes a compact chapter bar.</p>
        <p>Hover states cannot carry essential information. Touch targets, readable measure, spacing, and content priority become more important than preserving the exact desktop composition. Responsive design is treated as a transformation problem, not a shrinking problem.</p>
        
        <p>Motion follows the same logic. Animation can explain hierarchy, state, or movement through the system. It should not delay access to information. Where the operating system requests reduced motion, the interface respects that preference by disabling particle canvases, cursor tracking, and scroll-reveals.</p>
      </section>

      <!-- 06 PROBLEMS -->
      <section id="problems" class="chapter reveal">
        <h2 class="ch-kicker">06 &mdash; What Broke Along the Way</h2>
        <h3 class="ch-heading">Real migration logs, not hypothetical.</h3>
        <p>The difficult part was not choosing a framework. It was deciding what to preserve, what to rebuild, and where the boundaries actually belonged.</p>

        <div class="challenge-stack">
          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <h4 class="challenge-title">Challenge 01 &mdash; Migrating Without Flattening the Interface</h4>
              <div class="pdr-grid">
                <div class="pdr-label">PROBLEM</div>
                <div class="pdr-value">The content model needed to change without destroying the original visual identity.</div>
                <div class="pdr-label pdr-decision">DECISION</div>
                <div class="pdr-value">Preserve the stable visual layer and rebuild the architecture underneath it.</div>
                <div class="pdr-label pdr-result">RESULT</div>
                <div class="pdr-value">The site gained structured content and generated routes without becoming a generic framework rewrite.</div>
              </div>
            </div>
          </div>
          
          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <h4 class="challenge-title">Challenge 02 &mdash; Changing the Deployment Boundary</h4>
              <div class="pdr-grid">
                <div class="pdr-label">PROBLEM</div>
                <div class="pdr-value">After the Astro migration, source files were no longer equivalent to the finished website.</div>
                <div class="pdr-label pdr-decision">DECISION</div>
                <div class="pdr-value">Make <code>dist/</code> the explicit deployable artifact. Never deploy source directly.</div>
                <div class="pdr-label pdr-result">RESULT</div>
                <div class="pdr-value">Source, build, artifact, deployment, and delivery became separate failure boundaries.</div>
              </div>
            </div>
          </div>
          
          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <h4 class="challenge-title">Challenge 03 &mdash; Build Systems Expose Parser Boundaries</h4>
              <div class="pdr-grid">
                <div class="pdr-label">PROBLEM</div>
                <div class="pdr-value">Source that looked harmless as page copy broke the Astro production build when unescaped syntax crossed parser boundaries.</div>
                <div class="pdr-label pdr-decision">DECISION</div>
                <div class="pdr-value">Treat <code>npm run build</code> as part of development, not as the final publishing button.</div>
                <div class="pdr-label pdr-result">RESULT</div>
                <div class="pdr-value">Production parser failures became pre-deployment feedback instead of deployment surprises.</div>
              </div>
            </div>
          </div>

          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <h4 class="challenge-title">Challenge 04 &mdash; Mobile Is Not Desktop at a Smaller Size</h4>
              <div class="pdr-grid">
                <div class="pdr-label">PROBLEM</div>
                <div class="pdr-value">Desktop interactions did not always translate correctly to touch devices and narrower layouts.</div>
                <div class="pdr-label pdr-decision">DECISION</div>
                <div class="pdr-value">Change interaction behavior where necessary instead of protecting implementation uniformity.</div>
                <div class="pdr-label pdr-result">RESULT</div>
                <div class="pdr-value">Navigation, contact behavior, layout, and interactions became device-aware.</div>
              </div>
            </div>
          </div>
        </div>

        <h4 class="ch-sub" style="margin-top: 4rem;">MIGRATION LOG</h4>
        
        <div class="migration-log">
          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <div class="migration-entry-head">
                <span class="migration-date">2026-07-03</span>
                <a href="https://github.com/0xTaoZ/tzheng.dev/commit/9c1d3a2" target="_blank" rel="noopener noreferrer" class="migration-sha">9c1d3a2</a>
              </div>
              <strong class="migration-title">Migrate site to Astro content system</strong>
              <div class="migration-why">Moved from static HTML to Astro content collections, Zod schemas, and generated routes.</div>
            </div>
          </div>

          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <div class="migration-entry-head">
                <span class="migration-date">2026-07-03</span>
                <a href="https://github.com/0xTaoZ/tzheng.dev/commit/b806590" target="_blank" rel="noopener noreferrer" class="migration-sha">b806590</a>
              </div>
              <strong class="migration-title">Add GitHub Pages deployment workflow</strong>
              <div class="migration-why">Established automated CI/CD pipeline with GitHub Actions.</div>
            </div>
          </div>

          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <div class="migration-entry-head">
                <span class="migration-date">2026-07-03</span>
                <a href="https://github.com/0xTaoZ/tzheng.dev/commit/829e7b9" target="_blank" rel="noopener noreferrer" class="migration-sha">829e7b9</a>
              </div>
              <strong class="migration-title">Restore premium homepage with content-system routes</strong>
              <div class="migration-why">Preserved the original cinematic homepage through the new Astro architecture.</div>
            </div>
          </div>

          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <div class="migration-entry-head">
                <span class="migration-date">2026-07-03</span>
                <a href="https://github.com/0xTaoZ/tzheng.dev/commit/4eb4eff" target="_blank" rel="noopener noreferrer" class="migration-sha">4eb4eff</a>
              </div>
              <strong class="migration-title">Serve original premium homepage through Astro</strong>
              <div class="migration-why">Homepage served via Fragment set:html to maintain all original interactions.</div>
            </div>
          </div>

          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <div class="migration-entry-head">
                <span class="migration-date">2026-07-06</span>
                <a href="https://github.com/0xTaoZ/tzheng.dev/commit/dd321af" target="_blank" rel="noopener noreferrer" class="migration-sha">dd321af</a>
              </div>
              <strong class="migration-title">fix: resolve astro build syntax errors</strong>
              <div class="migration-why">Build parser caught unescaped backticks that silently existed in source.</div>
            </div>
          </div>

          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <div class="migration-entry-head">
                <span class="migration-date">2026-07-06</span>
                <a href="https://github.com/0xTaoZ/tzheng.dev/commit/440d761" target="_blank" rel="noopener noreferrer" class="migration-sha">440d761</a>
              </div>
              <strong class="migration-title">feat: improve mobile button layout</strong>
              <div class="migration-why">Changed email button from mailto to clipboard copy to prevent browser freeze.</div>
            </div>
          </div>

          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <div class="migration-entry-head">
                <span class="migration-date">2026-07-06</span>
                <a href="https://github.com/0xTaoZ/tzheng.dev/commit/caa226d" target="_blank" rel="noopener noreferrer" class="migration-sha">caa226d</a>
              </div>
              <strong class="migration-title">feat(system): overhaul architecture page</strong>
              <div class="migration-why">Ported cursor glow, particle canvas, 3D tilt, and reveal effects from homepage.</div>
            </div>
          </div>
        </div>

      </section>

      <!-- 07 SECURITY -->
      <section id="security" class="chapter reveal">
        <h2 class="ch-kicker">07 &mdash; Security Through Less Surface</h2>
        <h3 class="ch-heading">Security begins by removing infrastructure the system does not need.</h3>
        <p>Static architecture does not mean zero attack surface.</p>
        <p>The system still depends on account security, repository permissions, the dependency supply chain, CI/CD permissions, build integrity, hosting configuration, DNS, edge infrastructure, and client-side code.</p>
        <p>The security decision is narrower and more honest: <strong>Do not create a public runtime surface without a reason.</strong></p>
        <p>The goal is not to claim that risk disappears. The goal is to avoid operating public systems that the site does not need.</p>
        
        <div class="callout-block">Static-first is a reduction strategy, not a security guarantee.</div>
        
        <h4 class="ch-sub">TRUST SURFACE</h4>

        <div class="trust-row">
          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <h4 class="trust-title trust-exists">IDENTITY & ACCESS</h4>
              <ul class="trust-list">
                <li>GitHub Account</li>
                <li>Repository Permissions</li>
                <li>Branch / Deployment Permissions</li>
              </ul>
            </div>
          </div>
          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <h4 class="trust-title trust-exists">SUPPLY CHAIN</h4>
              <ul class="trust-list">
                <li>package.json</li>
                <li>package-lock.json</li>
                <li>npm Dependencies</li>
                <li>GitHub Actions</li>
                <li>Third-party Actions</li>
              </ul>
            </div>
          </div>
          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <h4 class="trust-title trust-exists">BUILD & DEPLOYMENT</h4>
              <ul class="trust-list">
                <li>GitHub Actions Workflow</li>
                <li>Scoped Permissions</li>
                <li>Build Artifact</li>
                <li>GitHub Pages</li>
              </ul>
            </div>
          </div>
          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <h4 class="trust-title trust-exists">DELIVERY</h4>
              <ul class="trust-list">
                <li>DNS Configuration</li>
                <li>Cloudflare Edge</li>
                <li>TLS / HTTPS</li>
              </ul>
            </div>
          </div>
          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <h4 class="trust-title trust-exists">CLIENT</h4>
              <ul class="trust-list">
                <li>Browser JavaScript</li>
                <li>DOM Interactions</li>
                <li>External Links</li>
              </ul>
            </div>
          </div>
        </div>

        <h4 class="ch-sub">CI/CD BOUNDARIES</h4>
        <p>The deployment workflow requests only the permissions required for its real job. It explicitly scopes the permissions required for GitHub Pages deployment instead of relying on unrestricted repository write access:</p>

        <div class="sys-card reveal" data-tilt style="margin-bottom: 2rem;">
          <div class="sys-card-inner pipeline">
            <div class="pipe-node">SOURCE (Repository)</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">BUILD IDENTITY (GitHub Actions)</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">SCOPED PERMISSIONS
              <ul class="perm-list">
                <li>contents: read</li>
                <li>pages: write</li>
                <li>id-token: write</li>
              </ul>
            </div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node">ARTIFACT (dist/)</div>
            <div class="pipe-sep">&darr;</div>
            <div class="pipe-node pipe-accent">DEPLOYMENT (GitHub Pages)</div>
          </div>
        </div>

        <a class="evidence-link" href="https://github.com/0xTaoZ/tzheng.dev/blob/main/.github/workflows/deploy.yml" target="_blank" rel="noopener noreferrer">
          View deployment workflow &nearr;
        </a>

        <h4 class="ch-sub">WHAT THE SYSTEM INTENTIONALLY AVOIDS</h4>
        <div class="sys-card reveal" data-tilt>
          <div class="sys-card-inner">
            <ul class="trust-list">
              <li>No runtime content database</li>
              <li>No public admin login</li>
              <li>No server-side user sessions</li>
              <li>No public user account system</li>
              <li>No comment backend</li>
              <li>No intentional advertising trackers</li>
            </ul>
          </div>
        </div>
        <p style="margin-top: 1.5rem;">These absences are not slogans. They are architectural decisions. Every missing runtime component is one less public system that needs credentials, patches, authentication logic, session handling, operational monitoring, and incident response.</p>

        <h4 class="ch-sub">RESIDUAL RISK</h4>
        <div class="sys-card reveal" data-tilt>
          <div class="sys-card-inner">
            <h4 class="trust-title trust-absent">WHAT STATIC ARCHITECTURE DOES NOT SOLVE</h4>
            <ul class="trust-list" style="columns: 1;">
              <li>Account takeover</li>
              <li>Repository compromise</li>
              <li>Malicious dependency updates</li>
              <li>CI/CD misconfiguration</li>
              <li>DNS compromise</li>
              <li>Edge configuration mistakes</li>
              <li>Client-side vulnerabilities</li>
              <li>Third-party resource risk</li>
            </ul>
          </div>
        </div>
        <p style="margin-top: 1.5rem;">A mature security posture should describe its residual risk rather than hide it. A compromised source repository, a malicious dependency, an unsafe workflow change, a DNS mistake, or vulnerable client-side code remain meaningful risks. Security is not one feature. It is the collection of trust boundaries the system chooses to keep.</p>

        <h4 class="ch-sub">TRADE-OFFS</h4>
        <div class="trust-row">
          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <h4 class="trust-title trust-exists">WHAT THE SYSTEM GAINS</h4>
              <ul class="trust-list">
                <li>Static delivery</li>
                <li>Version-controlled content</li>
                <li>Build-time validation</li>
                <li>Reproducible deployment</li>
                <li>Portable MDX files</li>
                <li>Simple rollback</li>
                <li>Smaller public runtime surface</li>
              </ul>
            </div>
          </div>
          <div class="sys-card reveal" data-tilt>
            <div class="sys-card-inner">
              <h4 class="trust-title trust-absent">WHAT THE SYSTEM GIVES UP</h4>
              <ul class="trust-list">
                <li>No live database editor</li>
                <li>No public user system</li>
                <li>No server-side comments</li>
                <li>A build step for every release</li>
                <li>More Git-oriented publishing</li>
                <li>Less runtime flexibility</li>
              </ul>
            </div>
          </div>
        </div>
        <p style="margin-top: 1.5rem;">Every simplification has a cost. The absence of a backend reduces the number of public systems that need to be operated. It also means publishing remains more closely tied to Git and the build pipeline. That trade-off is intentional today.</p>
        <p>The hybrid homepage creates another trade-off. Keeping custom global interactions preserves the visual identity of the site. It also means that part of the frontend is less componentized than the newer content system. That is accepted.</p>

        <div class="callout-block">A cleaner abstraction is not automatically a better engineering decision if creating it destroys something that already works.</div>
      </section>

      <!-- THE PRINCIPLE -->
      <div class="sys-card principle-card reveal" data-tilt style="margin-top: 6rem; margin-bottom: 2rem;">
        <div class="sys-card-inner">
          <h4 class="principle-title">THE PRINCIPLE</h4>
          <h2 class="principle-heading">Complexity has to earn its place.</h2>
          <div class="principle-body">
            <p>tzheng.dev is not designed to use the largest possible stack. It is designed so that every major layer has a reason to exist.</p>
            <p>Astro provides structure. MDX provides portable content. TypeScript strengthens contracts around the system. Git provides history and rollback. Automation turns source into a reproducible artifact. GitHub Pages serves the generated site. The edge layer supports public delivery. Custom frontend code remains where expression matters.</p>
            <div class="principle-rules">
              <p>Add structure where growth requires it.</p>
              <p>Preserve what already works.</p>
              <p>Make failure boundaries visible.</p>
              <p>Add complexity only when the problem justifies it.</p>
            </div>
          </div>
        </div>
      </div>

    </main>
  </div>
</BaseLayout>

<style is:global>
/* ===================================================================
   system.astro scoped styles
   Design system: mirrors index.html card, cursor, canvas, tilt logic
   =================================================================== */

/* --- Cursor glow (same CSS as index.html .cursor-field/.cursor-dot) --- */
.sys-cursor-field,
.sys-cursor-dot {
    position: fixed;
    left: 0;
    top: 0;
    z-index: 120;
    pointer-events: none;
    border-radius: 999px;
    opacity: 0;
    transform: translate3d(-50%, -50%, 0);
    transition: opacity .25s ease;
}
.sys-cursor-field {
    width: 21rem;
    height: 21rem;
    background: radial-gradient(circle, rgba(125,211,252,.14), rgba(57,255,136,.055) 36%, transparent 68%);
    mix-blend-mode: screen;
    filter: blur(2px);
}
.sys-cursor-dot {
    width: .42rem;
    height: .42rem;
    background: rgba(186,230,253,.95);
    box-shadow: 0 0 18px rgba(125,211,252,.85);
}
body.sys-pointer-ready .sys-cursor-field,
body.sys-pointer-ready .sys-cursor-dot {
    opacity: 1;
}

/* --- Ambient Canvas --- */
.sys-ambient-canvas {
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    opacity: 0.32;
    mix-blend-mode: screen;
    contain: strict;
    transform: translateZ(0);
}

/* --- Reveal animation (same as index.html) --- */
.reveal {
    opacity: 0;
    transform: translateY(60px) scale(0.95);
    transition: opacity 1s cubic-bezier(0.16, 1, 0.3, 1),
                transform 1s cubic-bezier(0.16, 1, 0.3, 1);
    will-change: opacity, transform;
}
.reveal.active {
    opacity: 1;
    transform: translateY(0) scale(1);
}

/* --- Hero --- */
.sys-hero {
    padding-top: 5rem;
    padding-bottom: 3rem;
    position: relative;
    z-index: 2;
}
.hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    gap: 2rem;
}
@media (min-width: 1024px) {
    .hero-grid {
        grid-template-columns: minmax(0, 1.08fr) minmax(320px, 0.92fr);
        align-items: center;
        gap: 4rem;
    }
}
.sys-title {
    font-size: clamp(3rem, 8vw, 6rem);
    line-height: 0.96;
    letter-spacing: -0.065em;
    font-weight: 700;
    color: #fff;
    margin: 1rem 0;
}
.sys-subtitle {
    color: #dbeafe;
    font-size: clamp(1.05rem, 2vw, 1.35rem);
    line-height: 1.6;
    font-weight: 300;
}
.narrative-text p {
    color: var(--muted);
    line-height: 1.65;
    margin-bottom: 1rem;
    font-size: 1.05rem;
    margin-top: 1.6rem;
}
.hero-tags {
    display: flex;
    flex-wrap: wrap;
    gap: .75rem;
    margin-top: 2.5rem;
}

/* --- Profile card specifics --- */
.profile-card { width: 100%; }
.profile-heading {
    font: 700 .72rem ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--muted);
    letter-spacing: .12em;
    margin: 0 0 1.2rem 0;
}
.profile-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: .9rem;
}
.profile-fact b {
    display: block;
    color: var(--muted);
    font: 500 .68rem ui-monospace, SFMono-Regular, Menlo, monospace;
    margin-bottom: .25rem;
}
.profile-fact span {
    color: #f8fafc;
    font-size: .88rem;
    font-weight: 500;
}

/* ===========================================================
   .sys-card — exact replication of index.html .project-case
   =========================================================== */
.sys-card {
    position: relative;
    overflow: hidden;
    border-radius: 1.35rem;
    border: 1px solid rgba(148,163,184,.16);
    background: linear-gradient(145deg, rgba(16,24,39,.72), rgba(2,6,14,.66));
    backdrop-filter: blur(22px) saturate(145%);
    -webkit-backdrop-filter: blur(22px) saturate(145%);
    transition: transform .65s cubic-bezier(.16,1,.3,1),
                border-color .45s ease,
                box-shadow .65s cubic-bezier(.16,1,.3,1),
                background .45s ease;
    transform-style: preserve-3d;
    will-change: transform;
    --tilt-x: 0deg;
    --tilt-y: 0deg;
    --mx: 50%;
    --my: 0%;
}

/* Mouse-tracking radial glow (::before) — same as .project-case::before */
.sys-card::before {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: radial-gradient(circle at var(--mx) var(--my), rgba(125,211,252,.18), transparent 20rem);
    opacity: 0;
    transition: opacity .35s ease;
}

/* Inner sheen layer (::after) — same as .project-case::after */
.sys-card::after {
    content: "";
    position: absolute;
    inset: 1px;
    border-radius: inherit;
    pointer-events: none;
    background:
        linear-gradient(135deg, rgba(255,255,255,.08), transparent 28%, transparent 72%, rgba(125,211,252,.055)),
        radial-gradient(circle at var(--mx) var(--my), rgba(57,255,136,.075), transparent 16rem);
    opacity: .42;
    transition: opacity .45s ease;
}

.sys-card:hover {
    border-color: rgba(125,211,252,.30);
    box-shadow: 0 30px 90px rgba(0,0,0,.46), 0 0 0 1px rgba(125,211,252,.05) inset;
}
.sys-card:hover::before { opacity: 1; }
.sys-card:hover::after { opacity: .85; }

/* 3D tilt transform — same logic as index.html */
@media (pointer: fine) and (min-width: 1024px) {
    .sys-card,
    .sys-card:hover {
        transform: perspective(1000px)
            rotateX(var(--tilt-x))
            rotateY(var(--tilt-y))
            translateY(var(--lift, 0px))
            scale(var(--card-scale, 1));
    }
    .sys-card:hover {
        --lift: -4px;
    }
}

.sys-card-inner {
    position: relative;
    z-index: 1;
    padding: clamp(1.25rem, 3vw, 2.25rem);
}

/* --- Mobile Chapter Bar --- */
.sys-glass-bar {
    position: sticky;
    top: 58px;
    z-index: 40;
    background: rgba(2, 5, 10, 0.62);
    backdrop-filter: saturate(180%) blur(30px);
    -webkit-backdrop-filter: saturate(180%) blur(30px);
    border-bottom: 1px solid rgba(148, 163, 184, 0.13);
    padding: 0.75rem 1rem;
    overflow-x: auto;
    white-space: nowrap;
}
.sys-glass-bar::-webkit-scrollbar { display: none; }
.chapter-list-mobile {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    gap: 1.5rem;
}
.chapter-list-mobile .ch-link {
    color: var(--muted);
    font: .8rem ui-monospace, SFMono-Regular, Menlo, monospace;
    padding: .3rem .6rem;
    border-radius: 6px;
    transition: all .3s ease;
}
.chapter-list-mobile .ch-link[aria-current="page"] {
    color: #fff;
    background: rgba(125,211,252,.15);
    box-shadow: 0 0 18px rgba(125,211,252,.35);
    border: 1px solid rgba(125,211,252,.25);
}

/* --- Two-column layout --- */
.sys-body {
    display: flex;
    flex-direction: column;
    gap: 4rem;
    padding-top: 2rem;
    padding-bottom: 6rem;
    position: relative;
    z-index: 2;
}
.rail-wrapper { display: none; }
@media (min-width: 1024px) {
    .sys-body { flex-direction: row; }
    .sys-lg-hidden { display: none !important; }
    .rail-wrapper {
        display: block;
        width: 220px;
        flex-shrink: 0;
    }
    .story-rail {
        position: sticky;
        top: 120px;
        max-height: calc(100vh - 140px);
        overflow-y: auto;
    }
}
.rail-label {
    font: .7rem ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--blue);
    letter-spacing: .12em;
    margin-bottom: 1.5rem;
}
.rail-track {
    position: relative;
    border-left: 1px solid rgba(148,163,184,.16);
    padding-left: 1.25rem;
}
.rail-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
}
.rail-list .ch-link {
    display: block;
    color: var(--muted);
    font-size: .9rem;
    font-weight: 500;
    transition: color .3s ease, transform .3s ease, text-shadow .3s ease;
    position: relative;
}
.rail-list .ch-link:hover {
    color: #fff;
    transform: translateX(5px);
}
.rail-list .ch-link[aria-current="page"] {
    color: #fff;
    transform: translateX(8px);
    text-shadow: 0 0 15px rgba(125,211,252,.8);
}
.rail-list .ch-link::before {
    content: '';
    position: absolute;
    left: -1.25rem;
    top: 50%;
    transform: translateY(-50%);
    width: 3px;
    height: 0;
    background: var(--blue);
    border-radius: 4px;
    box-shadow: 0 0 15px var(--blue);
    transition: height .3s ease;
}
.rail-list .ch-link[aria-current="page"]::before {
    height: 20px;
}

/* --- Story content --- */
.story-main {
    flex: 1;
    max-width: 720px;
}
.chapter {
    margin-bottom: 8rem;
    scroll-margin-top: 140px;
}
.ch-kicker {
    font: .9rem ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--blue);
    letter-spacing: .05em;
    margin: 0 0 1.2rem 0;
}
.ch-heading {
    font-size: 1.8rem;
    font-weight: 600;
    color: #fff;
    line-height: 1.3;
    margin: 0 0 2rem 0;
}
.chapter p {
    color: var(--muted);
    line-height: 1.7;
    margin-bottom: 1.25rem;
    font-size: 1.05rem;
    font-weight: 300;
}
.chapter code {
    color: var(--green);
    background: rgba(57,255,136,.08);
    border: 1px solid rgba(57,255,136,.14);
    border-radius: .45rem;
    padding: .1rem .35rem;
    font: .88rem ui-monospace, SFMono-Regular, Menlo, monospace;
}

/* --- Callout --- */
.callout-block {
    border-left: 3px solid var(--blue);
    padding: 1.2rem 1.5rem;
    margin: 2rem 0;
    color: #dbeafe;
    font-size: 1.1rem;
    font-weight: 500;
    background: linear-gradient(90deg, rgba(125,211,252,.08), transparent);
    border-radius: 0 12px 12px 0;
}

/* --- Before/After block --- */
.ba-grid {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    align-items: center;
}
@media (min-width: 640px) {
    .ba-grid { flex-direction: row; }
}
.ba-col {
    flex: 1;
    padding: 1.5rem;
    border: 1px solid rgba(148,163,184,.12);
    border-radius: 12px;
    background: rgba(2,6,14,.32);
}
.ba-after {
    border-color: rgba(125,211,252,.25);
    background: rgba(125,211,252,.06);
    box-shadow: 0 0 24px rgba(125,211,252,.08);
}
.ba-label {
    font: 700 .72rem ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--muted);
    letter-spacing: .1em;
    margin-bottom: .9rem;
}
.ba-label-accent { color: var(--blue); }
.ba-col ul {
    list-style: none;
    padding: 0;
    margin: 0;
}
.ba-col li {
    color: #cbd5e1;
    font-size: .9rem;
    margin-bottom: .55rem;
}
.ba-after li { color: #f8fafc; }
.ba-arrow {
    font-size: 1.5rem;
    color: var(--blue);
    text-shadow: 0 0 12px var(--blue);
}
@media (max-width: 639px) {
    .ba-arrow { transform: rotate(90deg); }
}

/* --- Architecture chip row --- */
.arch-intro { color: #f8fafc; margin-bottom: 1rem; }
.chip-row { display: flex; flex-wrap: wrap; gap: .5rem; }

/* --- Flow diagrams --- */
.flow-row {
    display: flex;
    flex-direction: column;
    gap: .5rem;
    align-items: center;
}
@media (min-width: 768px) {
    .flow-row {
        flex-direction: row;
        flex-wrap: wrap;
        justify-content: center;
        gap: 1rem;
    }
}
.flow-node {
    text-align: center;
    padding: 1rem 1.2rem;
    border: 1px solid rgba(148,163,184,.12);
    border-radius: 12px;
    background: rgba(2,6,14,.32);
    flex: 1;
    min-width: 100px;
}
.flow-node h4 {
    font: 700 .68rem ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--blue);
    letter-spacing: .1em;
    margin: 0 0 .5rem 0;
}
.flow-node span {
    color: #f8fafc;
    font-size: .85rem;
}
.flow-highlight {
    border-color: rgba(125,211,252,.35);
    background: rgba(125,211,252,.06);
    box-shadow: 0 0 20px rgba(125,211,252,.08);
}
.flow-sep {
    color: var(--blue);
    font-size: 1.2rem;
    text-shadow: 0 0 10px var(--blue);
}
@media (max-width: 767px) {
    .flow-sep { transform: rotate(90deg); }
}

/* --- Schema blocks --- */
.schema-row {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
    margin-top: 2.5rem;
}
@media (min-width: 640px) {
    .schema-row { grid-template-columns: 1fr 1fr; }
}
.schema-title {
    font: 700 .72rem ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--muted);
    letter-spacing: .1em;
    margin: 0 0 1rem 0;
    padding-bottom: .75rem;
    border-bottom: 1px solid rgba(148,163,184,.12);
}
.sys-card pre {
    margin: 0;
    font: .82rem/1.7 ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--green);
}

/* --- Pipeline block --- */
.pipeline {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: .4rem;
}
.pipe-node {
    font: 700 .8rem ui-monospace, SFMono-Regular, Menlo, monospace;
    color: #f8fafc;
    padding: .75rem 1.5rem;
    border: 1px solid rgba(148,163,184,.12);
    border-radius: 10px;
    background: rgba(2,6,14,.32);
}
.pipe-accent {
    border-color: rgba(125,211,252,.35);
    background: rgba(125,211,252,.08);
    color: var(--blue);
    box-shadow: 0 0 18px rgba(125,211,252,.12);
}
.pipe-sep {
    color: var(--blue);
    font-size: 1.1rem;
    text-shadow: 0 0 10px var(--blue);
}

/* --- Layer stack --- */
.layer-stack {
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
    text-align: center;
}
.layer-box {
    padding: 2rem;
    border: 1px solid rgba(148,163,184,.12);
    border-radius: 12px;
    background: rgba(2,6,14,.32);
}
.layer-identity {
    border-color: rgba(125,211,252,.3);
    background: rgba(125,211,252,.05);
    box-shadow: inset 0 0 20px rgba(125,211,252,.06);
}
.layer-box h4 {
    font: 700 .82rem ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--blue);
    margin: 0 0 .6rem 0;
}
.layer-box p {
    color: #f8fafc;
    font-size: .92rem;
    margin: 0;
}
.layer-content h4 { color: var(--muted); }
.layer-content p { color: #cbd5e1; }
.layer-sep {
    font-size: 1.5rem;
    color: var(--blue);
    text-shadow: 0 0 12px var(--blue);
}

/* --- Challenge blocks --- */
.challenge-stack {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    margin-top: 2rem;
}
.challenge-title {
    font-size: 1.15rem;
    color: #f8fafc;
    font-weight: 600;
    margin: 0 0 1.2rem 0;
}
.pdr-grid {
    display: grid;
    grid-template-columns: 80px 1fr;
    gap: .75rem 1rem;
    margin-top: 1rem;
    padding-top: 1rem;
    border-top: 1px solid rgba(148,163,184,.10);
}
.pdr-label {
    font: 700 .72rem ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--blue);
}
.pdr-decision { color: var(--green); }
.pdr-value {
    color: #cbd5e1;
    font-size: .92rem;
    line-height: 1.55;
}

/* --- Trust surface --- */
.trust-row {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
    margin-top: 2rem;
}
@media (min-width: 640px) {
    .trust-row { grid-template-columns: 1fr 1fr; }
}
.trust-title {
    font: 700 .76rem ui-monospace, SFMono-Regular, Menlo, monospace;
    letter-spacing: .1em;
    margin: 0 0 1rem 0;
}
.trust-exists { color: var(--blue); }
.trust-absent { color: var(--muted); }
.trust-list {
    list-style: none;
    padding: 0;
    margin: 0;
}
.trust-list li {
    color: #cbd5e1;
    font-size: .92rem;
    margin-bottom: .55rem;
}

/* --- Mobile adjustments --- */
@media (max-width: 900px) {
    .sys-card:hover {
        transform: none;
    }
    .sys-cursor-field,
    .sys-cursor-dot {
        display: none;
    }
}
@media (prefers-reduced-motion: reduce) {
    .sys-cursor-field,
    .sys-cursor-dot {
        display: none;
    }
    .reveal {
        opacity: 1;
        transform: none;
        transition: none;
    }
}

/* --- RESULT label in PDR grid --- */
.pdr-result { color: var(--violet, #c084fc); }

/* --- Migration log --- */
.migration-log {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-top: 2rem;
    padding-left: 1.25rem;
    border-left: 1px solid rgba(148,163,184,.16);
}
.migration-log .sys-card {
    border-radius: 1rem;
}
.migration-entry-head {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: .5rem .75rem;
    margin-bottom: .55rem;
}
.migration-date {
    font: 500 .72rem ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--muted);
}
.migration-sha {
    font: 500 .72rem ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--blue);
    text-decoration: none;
    border-bottom: 1px dashed rgba(125,211,252,.3);
    transition: color .2s ease;
}
.migration-sha:hover { color: #fff; }
.migration-title {
    color: #f8fafc;
    font-size: .92rem;
    font-weight: 600;
    display: block;
    margin-bottom: .35rem;
}
.migration-why {
    color: var(--muted);
    font-size: .85rem;
    line-height: 1.5;
}

/* --- Directory tree --- */
.dir-tree {
    font: .82rem/1.8 ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--green);
    background: transparent;
    border: none;
    padding: 0;
    margin: 0;
    white-space: pre;
}

/* --- Evidence link --- */
.evidence-link {
    display: inline-flex;
    align-items: center;
    gap: .4rem;
    margin-top: 1.5rem;
    font: 500 .78rem ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--blue);
    border-bottom: 1px dashed rgba(125,211,252,.3);
    transition: color .2s ease;
}
.evidence-link:hover { color: #fff; }

/* --- Workflow permissions --- */
.perm-list {
    list-style: none;
    padding: 0;
    margin: .5rem 0 0;
}
.perm-list li {
    font: .82rem/1.7 ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--green);
    padding: .15rem 0;
}

/* --- Principle block --- */
.principle-card {
    text-align: center;
    padding: clamp(2rem, 5vw, 4rem);
}
.principle-title {
    font: 700 .76rem ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--blue);
    letter-spacing: .12em;
    margin: 0 0 1.5rem 0;
}
.principle-heading {
    font-size: clamp(1.8rem, 4vw, 3rem);
    font-weight: 700;
    color: #fff;
    line-height: 1.15;
    letter-spacing: -.04em;
    margin: 0 0 2rem 0;
}
.principle-body p {
    color: var(--muted);
    line-height: 1.7;
    font-size: 1.05rem;
    font-weight: 300;
    margin-bottom: .75rem;
}
.principle-rules {
    margin-top: 2rem;
    display: flex;
    flex-direction: column;
    gap: .5rem;
}
.principle-rules p {
    color: #dbeafe;
    font-size: 1.05rem;
    font-weight: 500;
    margin: 0;
}

/* --- Sub-heading inside chapters --- */
.ch-sub {
    font: 700 .76rem ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--blue);
    letter-spacing: .08em;
    margin: 4rem 0 1.5rem 0;
}

/* --- Responsive transform card --- */
.transform-grid {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    align-items: center;
}
@media (min-width: 640px) {
    .transform-grid { flex-direction: row; }
}
.transform-col {
    flex: 1;
    padding: 1.5rem;
    border: 1px solid rgba(148,163,184,.12);
    border-radius: 12px;
    background: rgba(2,6,14,.32);
}
.transform-col ul {
    list-style: none;
    padding: 0;
    margin: 0;
}
.transform-col li {
    color: #cbd5e1;
    font-size: .88rem;
    margin-bottom: .5rem;
}
.transform-label {
    font: 700 .72rem ui-monospace, SFMono-Regular, Menlo, monospace;
    color: var(--muted);
    letter-spacing: .1em;
    margin-bottom: .9rem;
}
.transform-label-accent { color: var(--blue); }
.transform-accent {
    border-color: rgba(125,211,252,.25);
    background: rgba(125,211,252,.06);
}
.transform-accent li { color: #f8fafc; }
</style>

<script>
  // === System page interactive effects ===
  // Ported directly from index.html's DOMContentLoaded block.
  const initSystemEffects = () => {
    const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    const isMobile = () => window.matchMedia("(max-width: 768px)").matches;
    const finePointer = window.matchMedia("(pointer: fine)").matches;

    // ---------- 1. Cursor glow (exact index.html logic) ----------
    const pointer = {
      x: window.innerWidth / 2,
      y: window.innerHeight / 2,
      sx: window.innerWidth / 2,
      sy: window.innerHeight / 2
    };

    if (!reducedMotion && finePointer) {
      const cursorField = document.getElementById('sys-cursor-field');
      const cursorDot = document.getElementById('sys-cursor-dot');
      let cursorFrame: number | null = null;

      const renderPointer = () => {
        cursorFrame = null;
        pointer.sx += (pointer.x - pointer.sx) * 0.16;
        pointer.sy += (pointer.y - pointer.sy) * 0.16;
        const x = `${pointer.sx}px`;
        const y = `${pointer.sy}px`;
        if (cursorField) cursorField.style.transform = `translate3d(${x}, ${y}, 0) translate(-50%, -50%)`;
        if (cursorDot) cursorDot.style.transform = `translate3d(${pointer.x}px, ${pointer.y}px, 0) translate(-50%, -50%)`;
        if (Math.abs(pointer.x - pointer.sx) > 0.1 || Math.abs(pointer.y - pointer.sy) > 0.1) {
          cursorFrame = requestAnimationFrame(renderPointer);
        }
      };

      window.addEventListener('pointermove', (e: PointerEvent) => {
        pointer.x = e.clientX;
        pointer.y = e.clientY;
        document.body.classList.add('sys-pointer-ready');
        if (cursorFrame === null) cursorFrame = requestAnimationFrame(renderPointer);
      }, { passive: true });
      window.addEventListener('pointerleave', () => document.body.classList.remove('sys-pointer-ready'), { passive: true });
    }

    // ---------- 2. Ambient particle canvas (exact index.html logic) ----------
    const canvas = document.getElementById("sys-ambient") as HTMLCanvasElement | null;
    if (canvas && !reducedMotion) {
      const ctx = canvas.getContext("2d");
      if (ctx) {
        let width = 0;
        let height = 0;
        let particles: Array<{x: number; y: number; vx: number; vy: number; r: number}> = [];
        let raf: number | null = null;
        let resizeFrame: number | null = null;

        const resizeCanvas = () => {
          const dpr = Math.min(window.devicePixelRatio || 1, isMobile() ? 1.25 : 2);
          width = canvas.width = Math.floor(window.innerWidth * dpr);
          height = canvas.height = Math.floor(window.innerHeight * dpr);
          canvas.style.width = `${window.innerWidth}px`;
          canvas.style.height = `${window.innerHeight}px`;
          const count = isMobile() ? Math.min(34, Math.floor(window.innerWidth / 12)) : Math.min(80, Math.floor(window.innerWidth / 18));
          particles = Array.from({ length: count }, () => ({
            x: Math.random() * width,
            y: Math.random() * height,
            vx: (Math.random() - 0.5) * 0.16 * dpr,
            vy: (Math.random() - 0.5) * 0.16 * dpr,
            r: (Math.random() * 0.8 + 0.55) * dpr
          }));
        };

        const draw = () => {
          if (document.hidden) { raf = null; return; }
          ctx.clearRect(0, 0, width, height);
          const scale = width / Math.max(1, window.innerWidth);
          const px = pointer.sx * scale;
          const py = pointer.sy * scale;
          const glow = ctx.createRadialGradient(px, py, 0, px, py, 260 * scale);
          glow.addColorStop(0, "rgba(125, 211, 252, 0.105)");
          glow.addColorStop(0.42, "rgba(57, 255, 136, 0.045)");
          glow.addColorStop(1, "rgba(125, 211, 252, 0)");
          ctx.fillStyle = glow;
          ctx.fillRect(0, 0, width, height);
          ctx.fillStyle = "rgba(125, 211, 252, 0.38)";
          ctx.strokeStyle = "rgba(125, 211, 252, 0.10)";
          const linkDistance = (isMobile() ? 92 : 150) * Math.min(window.devicePixelRatio || 1, 2);
          particles.forEach((p, index) => {
            const mdx = px - p.x;
            const mdy = py - p.y;
            const md = Math.hypot(mdx, mdy);
            if (finePointer && md < 220 * scale) {
              const force = (1 - md / (220 * scale)) * 0.018;
              p.vx += mdx * force / Math.max(md, 1);
              p.vy += mdy * force / Math.max(md, 1);
            }
            p.vx *= 0.995;
            p.vy *= 0.995;
            p.x += p.vx;
            p.y += p.vy;
            if (p.x < 0 || p.x > width) p.vx *= -1;
            if (p.y < 0 || p.y > height) p.vy *= -1;
            ctx.beginPath();
            ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
            ctx.fill();
            for (let i = index + 1; i < particles.length; i++) {
              const q = particles[i];
              const dx = p.x - q.x;
              const dy = p.y - q.y;
              const distance = Math.hypot(dx, dy);
              if (distance < linkDistance) {
                ctx.globalAlpha = 1 - distance / linkDistance;
                ctx.beginPath();
                ctx.moveTo(p.x, p.y);
                ctx.lineTo(q.x, q.y);
                ctx.stroke();
                ctx.globalAlpha = 1;
              }
            }
          });
          raf = requestAnimationFrame(draw);
        };

        const restartCanvas = () => {
          if (raf !== null) cancelAnimationFrame(raf);
          resizeCanvas();
          draw();
        };
        restartCanvas();
        window.addEventListener("resize", () => {
          if (resizeFrame !== null) cancelAnimationFrame(resizeFrame);
          resizeFrame = requestAnimationFrame(() => { resizeFrame = null; restartCanvas(); });
        }, { passive: true });
        document.addEventListener("visibilitychange", () => {
          if (document.hidden && raf !== null) { cancelAnimationFrame(raf); raf = null; }
          else if (!document.hidden && raf === null) { restartCanvas(); }
        });
      }
    }

    // ---------- 3. Card tilt + mouse glow (exact index.html logic) ----------
    if (!reducedMotion && !isMobile()) {
      document.querySelectorAll('[data-tilt]').forEach((card) => {
        const el = card as HTMLElement;
        el.addEventListener('mousemove', (e: Event) => {
          const me = e as MouseEvent;
          const r = el.getBoundingClientRect();
          const x = me.clientX - r.left;
          const y = me.clientY - r.top;
          const rx = ((y / r.height) - 0.5) * -5;
          const ry = ((x / r.width) - 0.5) * 5;
          el.style.setProperty('--mx', `${(x / r.width) * 100}%`);
          el.style.setProperty('--my', `${(y / r.height) * 100}%`);
          el.style.setProperty('--tilt-x', `${rx}deg`);
          el.style.setProperty('--tilt-y', `${ry}deg`);
        });
        el.addEventListener('mouseleave', () => {
          el.style.setProperty('--tilt-x', '0deg');
          el.style.setProperty('--tilt-y', '0deg');
        });
      });
    }

    // ---------- 4. Reveal animation (same IntersectionObserver as index.html) ----------
    const revealObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (!entry.isIntersecting) return;
        entry.target.classList.add('active');
        revealObserver.unobserve(entry.target);
      });
    }, { root: null, rootMargin: '0px 0px -40px 0px', threshold: 0.1 });
    document.querySelectorAll('.reveal').forEach(el => revealObserver.observe(el));

    // ---------- 5. ScrollSpy navigation ----------
    const chapters = document.querySelectorAll('.chapter');
    const navLinksDesktop = document.querySelectorAll('.rail-list .ch-link');
    const navLinksMobile = document.querySelectorAll('.chapter-list-mobile .ch-link');

    if (chapters.length) {
      const updateNav = (id: string) => {
        const updateList = (links: NodeListOf<Element>) => {
          links.forEach((link: Element) => {
            if (link.getAttribute('href') === `#${id}`) {
              link.setAttribute('aria-current', 'page');
              if (link.closest('.chapter-list-mobile')) {
                const li = link.parentElement;
                if (li) li.scrollIntoView({ behavior: reducedMotion ? 'auto' : 'smooth', block: 'nearest', inline: 'center' });
              }
            } else {
              link.removeAttribute('aria-current');
            }
          });
        };
        updateList(navLinksDesktop);
        updateList(navLinksMobile);
        if (history.replaceState) {
          history.replaceState(null, "", `#${id}`);
        }
      };

      const scrollObserver = new IntersectionObserver((entries) => {
        const visibleEntry = entries.find(entry => entry.isIntersecting);
        if (visibleEntry) updateNav(visibleEntry.target.id);
      }, { root: null, rootMargin: '-20% 0px -60% 0px', threshold: 0 });

      chapters.forEach(chapter => scrollObserver.observe(chapter));
    }
  };

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initSystemEffects);
  } else {
    initSystemEffects();
  }
</script>
